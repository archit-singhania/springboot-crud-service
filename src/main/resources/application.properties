# SERVER CONFIGURATION
server.port=8080
spring.application.name=demo

# POSTGRESQL DATABASE CONFIGURATION
spring.datasource.url=jdbc:postgresql://localhost:5432/demo_db
spring.datasource.username=postgres
spring.datasource.password=2526
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / HIBERNATE CONFIGURATION
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ERROR HANDLING
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false
server.error.whitelabel.enabled=false
server.error.path=/error

# THYMELEAF CONFIG (only needed if you use HTML views)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# REDIS CONFIGURATION (for PKCE code_verifier storage)
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.timeout=60000
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0

# OKTA SSO CONFIGURATION
okta.oauth2.issuer=https://integrator-3041009.okta.com/oauth2/default
okta.oauth2.client-id=0oaxqnmiuyQSHHpeY697
okta.oauth2.client-secret=RNgQsvyZnVTP3syov1TdEcJswLL3p57XBm2cFKw3h7LiFbtBHd8aMiB_V6cY1Eyw

# Derived endpoints
okta.oauth2.authorization-endpoint=${okta.oauth2.issuer}/v1/authorize
okta.oauth2.token-endpoint=${okta.oauth2.issuer}/v1/token
okta.oauth2.jwks-uri=${okta.oauth2.issuer}/v1/keys
okta.oauth2.profile-endpoint=${okta.oauth2.issuer}/v1/userinfo

# Redirect URL
okta.oauth2.redirect-uri=http://localhost:8080/sso/callback

# Scopes
okta.oauth2.scope=openid profile email organization

# LOGGING CONFIGURATION
logging.level.com.example.demo=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

# Custom Exchange Token Configuration
exchange.token.issuer=https://etp.cpcb.gov.in
exchange.token.expiry-minutes=15
exchange.token.refresh-expiry-hours=24